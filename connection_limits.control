# connection limits (quotas)
comment = 'Allows setting various connection quotas (per user, db or IP address).'
default_version = '1.0.0'
relocatable = true

module_pathname = '$libdir/connection_limits'
